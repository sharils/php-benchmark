#!/usr/bin/env php
<?php
require __DIR__ . '/../vendor/autoload.php';

$phrofiler = new Sharils\Phrofiler();
$phrofiler->setUp($argv[1]);
$phrofiler->tearDown($argv[2]);
$profiles = $phrofiler->profile(array_slice($argv, 3));
foreach ($profiles as $profile) {
    $profile->php = "php -r '{$argv[1]}; $profile->snippet; {$argv[2]};'";
    $profile->ratio = round($profile->time / $profiles[0]->time * 100) . '%';
    $profile->time .= ' (average seconds)';
    foreach (['snippet', 'timeFilename', 'php', 'time', 'ratio'] as $profileKey) {
        printf("%12s: %s\n", $profileKey, $profile->$profileKey);
    }
    echo PHP_EOL;
}
